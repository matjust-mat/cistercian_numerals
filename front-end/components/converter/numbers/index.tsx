'use client'

interface NumbersProps {
  from?: boolean;
  to?: boolean;
}

export default function Numbers({ from, to }: NumbersProps) {
  return (
    <div className="min-w-96 min-h-96 bg-tint-orange">
    </div>
  );
};