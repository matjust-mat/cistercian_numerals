'use client'

interface CirtecianProps {
  from?: boolean;
  to?: boolean;
};

export default function Cirtecian({ from, to }: CirtecianProps) {
  return (
    <div className="min-w-96 min-h-96 bg-shade-orange">
    </div>
  );
};